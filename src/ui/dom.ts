export function field(label: string, control: HTMLElement): HTMLElement { const field = el("label", "field"); field.append(el("span", "field-label", label), control); return field; }
export function input(id: string, placeholder: string, type: string, value: string): HTMLInputElement { const input = document.createElement("input"); input.id = id; input.name = id; input.type = type; input.placeholder = placeholder; input.value = value; input.required = type !== "search"; return input; }
export function select(label: string, options: string[][], value: string): HTMLSelectElement { const select = document.createElement("select"); select.setAttribute("aria-label", label); for (const [key, text] of options) { const option = document.createElement("option"); option.value = key; option.textContent = text; select.append(option); } if (options.some(([key]) => key === value)) select.value = value; return select; }
export function checkbox(label: string, checked: boolean, change: (value: boolean) => void): HTMLElement { const wrap = el("label", "checkbox"); const input = document.createElement("input"); input.type = "checkbox"; input.checked = checked; input.addEventListener("change", () => change(input.checked)); wrap.append(input, el("span", "", label)); return wrap; }
export function button(text: string, className: string, type: "button" | "submit"): HTMLButtonElement { const button = el("button", `button ${className}`, text) as HTMLButtonElement; button.type = type; return button; }
export function el<K extends keyof HTMLElementTagNameMap>(tag: K, className = "", text?: string): HTMLElementTagNameMap[K] { const node = document.createElement(tag); if (className) node.className = className; if (text !== undefined) node.textContent = String(text ?? ""); return node; }

